# Required for the soft meal analysis route (app/api/ai/meal-analysis).
# Leave empty to disable the feature gracefully; the UI will show a soft notice.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini if unset.
# OPENAI_MODEL=gpt-4o-mini
